import { CedinsiDirectivaDirective } from './cedinsi-directiva.directive';

describe('CedinsiDirectivaDirective', () => {
  it('should create an instance', () => {
    const directive = new CedinsiDirectivaDirective();
    expect(directive).toBeTruthy();
  });
});
