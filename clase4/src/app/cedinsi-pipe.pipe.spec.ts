import { CedinsiPipePipe } from './cedinsi-pipe.pipe';

describe('CedinsiPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CedinsiPipePipe();
    expect(pipe).toBeTruthy();
  });
});
